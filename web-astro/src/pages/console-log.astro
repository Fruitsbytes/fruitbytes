---
// PORT TARGET: web-stencil/src/components/right-panel console viewer +
//              web-stencil/src/components/console-welcome
// The /console-log route renders the live console viewer as the page's
// main content. Logs are dispatched into the shared signal store from
// anywhere in the app (e.g. event listeners, navigation, errors).
import Layout from '../layouts/Layout.astro';
import ConsoleViewer from '../components/shell/ConsoleViewer.tsx';
---

<Layout title="Console — FruitsBytes">
  <ConsoleViewer client:load />
</Layout>
